const baseUrl = "/api";

const _routes = {
  login: `${baseUrl}/Accounts/login`,
  registerVendor: `${baseUrl}/Accounts/registerVendor`,
  registerCustomer: `${baseUrl}/Accounts/registerCustomer`,
  serviceBooking: `${baseUrl}/`,
};

export default _routes;
